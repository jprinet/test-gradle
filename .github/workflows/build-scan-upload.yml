name: PR Build Scan Upload

on:
  workflow_run:
    workflows: [ "PR Build" ]
    types: [ completed ]
#  pull_request:
#    types: [ opened,closed,synchronize ]
#  issue_comment:
#    types: [ created ]

jobs:
#  GE-TOS:
#    uses: jprinet/test-gradle/.github/workflows/reusable-build-scan-tos.yml
#    secrets: inherit

  publish-gradle-build-scans:
#    needs: GE-TOS
    uses: jprinet/test-gradle/.github/workflows/reusable-build-scan-upload-gradle.yml
    secrets: inherit

  publish-maven-build-scans:
#    needs: GE-TOS
    uses: jprinet/test-gradle/.github/workflows/reusable-build-scan-upload-maven.yml
    secrets: inherit
